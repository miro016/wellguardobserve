# Active reconnaissance tools

These packages directly probe targets approved by collection scope and budget
policy. They cover host and port discovery, HTTP and SMTP probing, TLS and
technology fingerprinting, and the isolated GoScans actor.

This is a navigation folder, not a Go package. Each child package remains an
independent actor with its own configuration, results, and operational events.
Authorization and scheduling belong to orchestration rather than to this
folder.
